name: friendly_id
version: 0.1.0

authors:
  - Daniel Calixto calixtodaniel@gmail.com

crystal: ">= 1.0.0"

description: |
  FriendlyId for Crystal - Create human-readable URLs and slugs

dependencies:
  db:
    github: crystal-lang/crystal-db
development_dependencies:
  spectator:
    github: icy-arctic-fox/spectator
  sqlite3:
    github: crystal-lang/crystal-sqlite3

scripts:
  postinstall: crystal src/friendly_id/install.cr
